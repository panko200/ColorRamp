<UserControl x:Class="ColorRamp.GradientCurveEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="220" d:DesignHeight="220">
    <!--
        グラデーション色補間カーブエディタ。
        ・左クリック (空き): 点を追加
        ・左クリックドラッグ (点の上): 点を移動
        ・右クリック (内部点): 点を削除 (端点は削除不可)
        ・端点は X 固定 (0 と 1)、Y のみ動かせる
    -->
    <Border BorderBrush="#555" BorderThickness="1" CornerRadius="2">
        <Canvas x:Name="CurveCanvas"
                Background="#1a1a1a"
                
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                MouseRightButtonDown="Canvas_MouseRightButtonDown"
                SizeChanged="Canvas_SizeChanged"/>
    </Border>
</UserControl>
